/**
 * PostCSS configuration.
 * NOTE: This project uses Tailwind CSS v4 via @tailwindcss/vite which processes
 * CSS through Vite directly. PostCSS is not required for the build pipeline.
 * This file exists for tooling compatibility (e.g. IDE plugins, stylelint).
 */
export default {
  plugins: {},
};
